# =============================================================================
# Course Chat Widget - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit your .env file to version control!

# =============================================================================
# AI PROVIDER CONFIGURATION (required)
# =============================================================================
# Configure ONE AI provider to use for all course bots. This is a global setting.
#
# Step 1: Set the API key for your chosen provider (at least one required)
# Step 2: Set LLM_PROVIDER and LLM_MODEL to specify which provider/model to use
#
# Available providers and models:
# - Anthropic: claude-sonnet-4-20250514, claude-opus-4-20250514, claude-3-5-haiku-20241022
# - OpenAI: gpt-4o, gpt-4o-mini, gpt-4-turbo
# - Google: gemini-1.5-pro, gemini-1.5-flash, gemini-1.0-pro

# Anthropic (Claude) - https://console.anthropic.com/settings/keys
# Recommended for best prompt caching and cost efficiency
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI (GPT) - https://platform.openai.com/api-keys
# Uncomment to enable OpenAI
# OPENAI_API_KEY=sk-proj-your-key-here

# Google (Gemini) - https://aistudio.google.com/apikey
# Uncomment to enable Google Gemini
# GOOGLE_API_KEY=AIzaSy-your-key-here

# =============================================================================
# LLM MODEL SELECTION (required)
# =============================================================================
# Set these to choose which provider and model ALL course bots will use.
# The provider must have an API key configured above.

LLM_PROVIDER=anthropic
LLM_MODEL=claude-sonnet-4-20250514

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Note: Admin credentials are set up via the web interface on first launch.
# Visit /setup to create your admin account.

# Comma-separated list of allowed origins for CORS
# In production, set this to your specific domains
# Example: https://yourdomain.com,https://courses.yourdomain.com
ALLOWED_ORIGINS=*

# Rate limiting: max chat requests per IP per minute
RATE_LIMIT_PER_MINUTE=20

# Maximum course content length in characters (default: 500,000)
MAX_CONTENT_LENGTH=500000

# Maximum chat message length in characters (default: 5,000)
MAX_MESSAGE_LENGTH=5000

# Maximum conversation history messages to include (default: 20)
MAX_CONVERSATION_HISTORY=20

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Enable debug mode (shows /docs and /redoc endpoints)
# Set to false in production!
DEBUG=true

# Port to run on (used by deployment platforms)
PORT=8000

# Database URL (defaults to SQLite)
# For PostgreSQL: postgresql://user:password@host:port/database
DATABASE_URL=sqlite:///./courses.db
